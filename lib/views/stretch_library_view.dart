import 'package:flutter/material.dart';

class StretchLibraryView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stretch Library'),
      ),
      body: Container(),
    );
  }
}
