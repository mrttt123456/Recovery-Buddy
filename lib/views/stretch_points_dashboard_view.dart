import 'package:flutter/material.dart';

class StretchPointsDashboardView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stretch Points Dashboard'),
      ),
      body: Container(),
    );
  }
}
