import 'package:flutter/material.dart';

class RecoveryPlanCreatorView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Recovery Plan Creator'),
      ),
      body: Container(),
    );
  }
}
